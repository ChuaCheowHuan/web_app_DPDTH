language: python
python:
    - 3.6
services:
    - docker
install:
    - pip install -r requirements.txt
script:
    - docker-compose build
    #- docker-compose up
    - docker-compose run web python manage.py test
